# Lightweight requirements for Railway deployment (CPU-only PyTorch)
# Use this for cloud deployment to avoid build timeouts

# Core scientific computing
numpy>=1.21.0
pandas>=1.3.0

# Image processing (HEADLESS - no GUI dependencies)
Pillow>=8.3.0
opencv-python-headless==4.10.0.84  # Headless version without libGL

# Machine Learning
scikit-learn>=0.24.0

# PyTorch CPU-only (much faster to install)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.0.0+cpu
torchvision==0.15.0+cpu

# Web API
flask>=2.3.0
flask-cors>=4.0.0
gunicorn>=21.0.0

# Progress bars
tqdm>=4.62.0
